imo;

// change-password service
import changePassService from "./forgot-password/changePass.service";

const changePasswordController = async (req, res) => {
  const { oldPassword, newPassword } = req.body;
};
